---
title: ナビゲーションを描画する
description: ctx.pagesTree からサイドバー/メニューを組み立てる。
weight: 3
starryNight: true
---

# ナビゲーションを描画する

Methanol は `ctx.pagesTree` にナビゲーションツリーを用意します。これは現在のナビゲーションルート（`isRoot`）にスコープされ、hidden/exclude のルールも反映済みです。

各ノードの例:

```js
{
	type: 'page' | 'directory',
	title: '...',
	name: '...',
	routePath: '/guide/',
	routeHref: '/docs/guide/', // base 対応
	children: []
}
```

## 最小のサイドバー

```jsx
const NavTree = ({ nodes, currentHref }) => (
	<ul>
		{(nodes || []).map((node) => {
			const label = node.title || node.name || node.routePath
			if (node.type === 'directory') {
				return (
					<li>
						{node.routeHref ? <a href={node.routeHref}>{label}</a> : <span>{label}</span>}
						{node.children?.length ? <NavTree nodes={node.children} currentHref={currentHref} /> : null}
					</li>
				)
			}
			const isActive = node.routeHref && node.routeHref === currentHref
			return (
				<li>
					<a href={node.routeHref} aria-current={isActive ? 'page' : null}>
						{label}
					</a>
				</li>
			)
		})}
	</ul>
)

export default function PageTemplate({ PageContent, ExtraHead, ctx }) {
	return (
		<>
			<html>
				<head>
					<ExtraHead />
				</head>
				<body>
					<aside>
						<NavTree nodes={ctx.pagesTree} currentHref={ctx.page.routeHref} />
					</aside>
					<main>
						<PageContent />
					</main>
				</body>
			</html>
		</>
	)
}
```

ヒント:

- リンクには `routeHref` を使うのが安全です（`site.base` を含みます）。
- ディレクトリ index のルートは `routePath` が末尾 `/` 付きになります（例: `/guide/`）。

